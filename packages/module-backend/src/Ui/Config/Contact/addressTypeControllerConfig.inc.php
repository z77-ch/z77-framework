<?php
/**
 * Backend mount of the address-type fragment (plan §4a, ADR-018 pattern): pin
 * the page body to the fragment's `listAction` template in `module-contact`.
 */
return \Z77\Module\Contact\Ui\AddressTypeLayout::config();
