<?php
/**
 * Backend mount of the fiscal-year fragment (plan §5.1, ADR-018 pattern):
 * pin the page body to the fragment's `listAction` template in
 * `module-financial`.
 */
return \Z77\Module\Financial\Ui\FiscalYearLayout::config();
