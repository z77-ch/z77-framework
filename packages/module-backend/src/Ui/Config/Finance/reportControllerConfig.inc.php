<?php
/**
 * Backend mount of the report fragment (plan §5.5, ADR-018 pattern): pin the
 * page body to the fragment's trial balance in `module-financial`.
 */
return \Z77\Module\Financial\Ui\ReportLayout::config();
