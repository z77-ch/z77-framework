<?php
/**
 * Backend mount of the journal fragment (plan §5.2, ADR-018 pattern): pin the
 * page body to the fragment's `listAction` template in `module-financial`.
 */
return \Z77\Module\Financial\Ui\JournalLayout::config();
